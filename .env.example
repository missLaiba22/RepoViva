# Database
DATABASE_URL=postgresql+psycopg://repoviva:repoviva_dev@localhost:5432/repoviva

# App
ENV=development
# Where cloned repositories live on disk during ingestion. Treated as
# ephemeral — do NOT point this at a persistent volume that must survive
# restarts; the fetcher re-clones on demand.
WORKSPACE_ROOT=./workspace